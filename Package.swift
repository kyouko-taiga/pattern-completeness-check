// swift-tools-version: 5.7
import PackageDescription

let package = Package(
  name: "PatternCompleteness",
  dependencies: [],
  targets: [
    .executableTarget(
      name: "PatternCompleteness",
      dependencies: []),
  ])
